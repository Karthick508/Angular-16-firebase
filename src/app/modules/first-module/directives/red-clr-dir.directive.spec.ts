import { RedClrDirDirective } from './red-clr-dir.directive';

describe('RedClrDirDirective', () => {
  it('should create an instance', () => {
    const directive = new RedClrDirDirective();
    expect(directive).toBeTruthy();
  });
});
